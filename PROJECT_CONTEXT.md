📝 FamilyHub 项目上下文与规划文档

1. 项目背景
   目标：开发一个面向家庭成员（爸爸、妈妈、宝宝、舅舅）的 H5 游戏聚合平台。

首发游戏：你画我猜（你画我猜）。

核心场景：

多端参与：成员使用手机或平板同时在线。

低门槛交互：针对 6 岁宝宝（一年级，识字有限）和长辈优化，采用“口头猜词 + 线下评分”模式。

部署环境：仅局域网使用，由一台常开电脑作为服务器。

2. 技术栈架构
   前端：Vue 3 (Setup) + Vite + Tailwind CSS v4 + Varlet UI。

状态管理：Pinia。

实时通讯：Socket.io (Websocket 模式)。

后端：Node.js (Express)。

核心算法：2000x2000 归一化逻辑坐标系，确保不同分辨率设备（iPad/手机）画作同步不偏移。

规范：项目采用 ES Module (ESM) 规范 ("type": "module")。

3. 当前开发进度 (v0.1 MVP)
   ✅ 基础架构：前后端项目初始化，支持局域网访问。

✅ 画板同步：实现跨端坐标归一化绘制及清空逻辑。

✅ 状态管理：Pinia 已定义玩家列表、分数、游戏状态（waiting/playing/result）等字段。

✅ UI 交互：完成角色选择（爸爸/妈妈等）、画手工具栏、计分板基础展示。

4. 待办规划 (v0.2 自动化与辅助增强)
   优先级 A：游戏生命周期闭环
   后端计时器：完善 server/index.js 中的 startTimer，实现 60s 倒计时广播。

回合流转：实现 nextRound 逻辑，自动随机抽词、轮换画手。

状态切换：完成 playing (绘图) 到 result (结算) 的自动转换。

优先级 B：宝宝适配与 UX
语音辅助 (TTS)：集成 window.speechSynthesis，新回合开始时为画手（宝宝）语音播报题目。

拼音增强：优化题目区域，确保汉字与拼音大字体显示。

得分反馈：实现 score_animate 动效，增加加分时的视觉激励。

优先级 C：健壮性修复
模块化修复：将 server/index.js 从 CommonJS (require) 彻底迁移至 ESM (import)，修复运行报错。

重连同步：实现用户刷新页面后自动通过 join_game 恢复角色状态。

5. 开发准则
   架构风格：代码注释要求专业干练，严禁冗长，拒绝“人机感”过强的描述。

变更范围：仅修改逻辑相关部分，严禁随意重构已跑通的 GameBoard 归一化逻辑或 UI 结构。

多端优先：所有 UI 变动需考虑移动端触摸手势及小屏幕适配。
